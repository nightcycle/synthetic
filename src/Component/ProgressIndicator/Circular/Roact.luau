--!strict
-- this script was generated by the build system, please do not make manual edits
local _Package = script.Parent.Parent.Parent.Parent
local _Packages = _Package.Parent
-- Services
-- Packages
local Maid = require(_Packages:WaitForChild("Maid"))
local ColdFusion = require(_Packages:WaitForChild("ColdFusion"))
local Roact = require(_Packages:WaitForChild("Roact"))

-- Modules
local Types = require(_Package:WaitForChild("Types"))
local Style = require(_Package:WaitForChild("Style"))

local Source = require(script.Parent:WaitForChild("ColdFusion"))

-- Types
type Maid = Maid.Maid
type Style = Style.Style
type FontData = Types.FontData
type OptionData = Types.OptionData
type ButtonData = Types.ButtonData
type ImageData = Types.ImageData
type CanBeState<V> = ColdFusion.CanBeState<V>
-- Constants
local DEFAULTS = require(script.Parent:WaitForChild("Defaults"))
-- Variables
-- References
-- Private Functions
-- Class
local Interface = {}

do
	local NewProgressIndicatorCircular = Roact.Component:extend("NewProgressIndicatorCircular")
	NewProgressIndicatorCircular.defaultProps = {
		progress = DEFAULTS.NEW.PROGRESS,
		isEnabled = DEFAULTS.NEW.IS_ENABLED,
		fillColor = DEFAULTS.NEW.FILL_COLOR,
		emptyColor = DEFAULTS.NEW.EMPTY_COLOR,
		scale = DEFAULTS.NEW.SCALE,
	}
	function NewProgressIndicatorCircular:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			progress = _Value(self.defaultProps.progress),
			isEnabled = _Value(self.defaultProps.isEnabled),
			fillColor = _Value(self.defaultProps.fillColor),
			emptyColor = _Value(self.defaultProps.emptyColor),
			scale = _Value(self.defaultProps.scale),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.new(
			self._internalStates.progress,
			self._internalStates.isEnabled,
			self._internalStates.fillColor,
			self._internalStates.emptyColor,
			self._internalStates.scale
		)
	end

	function NewProgressIndicatorCircular:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return Roact.createElement(Roact.Portal, props)
	end
	function NewProgressIndicatorCircular:didMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function NewProgressIndicatorCircular:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.New = NewProgressIndicatorCircular
end

do
	local PrimaryProgressIndicatorCircular = Roact.Component:extend("PrimaryProgressIndicatorCircular")
	PrimaryProgressIndicatorCircular.defaultProps = {
		style = DEFAULTS.PRIMARY.STYLE,
		progress = DEFAULTS.PRIMARY.PROGRESS,
		isEnabled = DEFAULTS.PRIMARY.IS_ENABLED,
	}
	function PrimaryProgressIndicatorCircular:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			progress = _Value(self.defaultProps.progress),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.primary(
			self._internalStates.style,
			self._internalStates.progress,
			self._internalStates.isEnabled
		)
	end

	function PrimaryProgressIndicatorCircular:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return Roact.createElement(Roact.Portal, props)
	end
	function PrimaryProgressIndicatorCircular:didMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function PrimaryProgressIndicatorCircular:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.Primary = PrimaryProgressIndicatorCircular
end

do
	local SecondaryProgressIndicatorCircular = Roact.Component:extend("SecondaryProgressIndicatorCircular")
	SecondaryProgressIndicatorCircular.defaultProps = {
		style = DEFAULTS.SECONDARY.STYLE,
		progress = DEFAULTS.SECONDARY.PROGRESS,
		isEnabled = DEFAULTS.SECONDARY.IS_ENABLED,
	}
	function SecondaryProgressIndicatorCircular:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			progress = _Value(self.defaultProps.progress),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.secondary(
			self._internalStates.style,
			self._internalStates.progress,
			self._internalStates.isEnabled
		)
	end

	function SecondaryProgressIndicatorCircular:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return Roact.createElement(Roact.Portal, props)
	end
	function SecondaryProgressIndicatorCircular:didMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function SecondaryProgressIndicatorCircular:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.Secondary = SecondaryProgressIndicatorCircular
end

do
	local TertiaryProgressIndicatorCircular = Roact.Component:extend("TertiaryProgressIndicatorCircular")
	TertiaryProgressIndicatorCircular.defaultProps = {
		style = DEFAULTS.TERTIARY.STYLE,
		progress = DEFAULTS.TERTIARY.PROGRESS,
		isEnabled = DEFAULTS.TERTIARY.IS_ENABLED,
	}
	function TertiaryProgressIndicatorCircular:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			progress = _Value(self.defaultProps.progress),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.tertiary(
			self._internalStates.style,
			self._internalStates.progress,
			self._internalStates.isEnabled
		)
	end

	function TertiaryProgressIndicatorCircular:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return Roact.createElement(Roact.Portal, props)
	end
	function TertiaryProgressIndicatorCircular:didMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function TertiaryProgressIndicatorCircular:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.Tertiary = TertiaryProgressIndicatorCircular
end

return Interface
