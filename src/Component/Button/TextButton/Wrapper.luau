--!strict
-- this script was generated by the build system, please do not make manual edits
local _Package = script.Parent.Parent.Parent.Parent
local _Packages = _Package.Parent
-- Services
-- Packages
local Maid = require(_Packages:WaitForChild("Maid"))
local ColdFusion = require(_Packages:WaitForChild("ColdFusion"))

-- Modules
local Types = require(_Package:WaitForChild("Types"))
local Style = require(_Package:WaitForChild("Style"))
local Enums = require(_Package:WaitForChild("Enums"))
local Translators = require(_Package:WaitForChild("Translators"))

local Source = require(script.Parent:WaitForChild("ColdFusion"))

-- Types
type Maid = Maid.Maid
type Style = Style.Style
type FontData = Types.FontData
type OptionData = Types.OptionData
type ButtonData = Types.ButtonData
type ImageData = Types.ImageData
type CanBeState<V> = ColdFusion.CanBeState<V>
type Wrapper<BaseInstance, Definition, ClassName> = Translators.Wrapper<BaseInstance, Definition, ClassName>

export type TextButtonWrapperDefinition = {
	OnClick: () -> (),
	Text: string,
	Icon: ImageData?,
	TextColor: Color3,
	DisabledTextColor: Color3,
	IsEnabled: boolean,
	Elevation: number,
	SchemeType: Enums.SchemeType,
	FontData: FontData,
	Scale: number,
}
export type TextButtonWrapper = Wrapper<GuiObject, TextButtonWrapperDefinition, "TextButton">

export type StyledTextButtonWrapperDefinition = {
	Style: Style,
	OnClick: () -> (),
	Text: string,
	Icon: ImageData?,
	Elevation: number,
	IsEnabled: boolean,
}
export type StyledTextButtonWrapper = Wrapper<
	GuiObject,
	StyledTextButtonWrapperDefinition,
	"StyledTextButton"
>
-- Constants
-- Variables
-- References
-- Private Functions
-- Interface
local Interface = {}

function Interface.primary(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.primary(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

function Interface.secondary(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.secondary(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

function Interface.tertiary(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.tertiary(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

function Interface.primaryContainer(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.primaryContainer(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

function Interface.secondaryContainer(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.secondaryContainer(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

function Interface.tertiaryContainer(): StyledTextButtonWrapper
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local _Value = _fuse.Value
	local definition = {
		Style = _Value(Style.new(1, Enum.Font.SourceSans, "Light", Color3.new(0, 0.4, 0.7))),
		OnClick = _Value(function() end),
		Text = _Value("Button"),
		Icon = _Value(nil),
		Elevation = _Value(0),
		IsEnabled = _Value(true),
	}
	local inst: GuiObject = Source.tertiaryContainer(
		definition.Style,
		definition.OnClick,
		definition.Text,
		definition.Icon,
		definition.Elevation,
		definition.IsEnabled
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	local wrapper, cleanUp = Translators.ColdFusion.toWrapper("StyledTextButton", inst, definition)
	maid:GiveTask(cleanUp)
	return wrapper
end

return Interface
