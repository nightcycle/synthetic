--!strict
-- this script was generated by the build system, please do not make manual edits
local _Package = script.Parent.Parent.Parent.Parent
local _Packages = _Package.Parent
-- Services
-- Packages
local Maid = require(_Packages:WaitForChild("Maid"))
local ColdFusion = require(_Packages:WaitForChild("ColdFusion"))

local React = require(_Packages:WaitForChild("React"))
local ReactRoblox = require(_Packages:WaitForChild("ReactRoblox"))

-- Modules
local Types = require(_Package:WaitForChild("Types"))
local Style = require(_Package:WaitForChild("Style"))

local Source = require(script.Parent:WaitForChild("ColdFusion"))

-- Types
type Maid = Maid.Maid
type Style = Style.Style
type FontData = Types.FontData
type OptionData = Types.OptionData
type ButtonData = Types.ButtonData
type ImageData = Types.ImageData
type CanBeState<V> = ColdFusion.CanBeState<V>
-- Constants
local DEFAULTS = require(script.Parent:WaitForChild("Defaults"))
-- Variables
-- References
-- Private Functions
-- Class
local Interface = {}

do
	local NewButtonOutlinedButton = React.Component:extend("NewButtonOutlinedButton")
	NewButtonOutlinedButton.defaultProps = {
		onClick = DEFAULTS.NEW.ON_CLICK,
		text = DEFAULTS.NEW.TEXT,
		icon = DEFAULTS.NEW.ICON,
		textColor = DEFAULTS.NEW.TEXT_COLOR,
		disabledTextColor = DEFAULTS.NEW.DISABLED_TEXT_COLOR,
		isEnabled = DEFAULTS.NEW.IS_ENABLED,
		elevation = DEFAULTS.NEW.ELEVATION,
		schemeType = DEFAULTS.NEW.SCHEME_TYPE,
		fontData = DEFAULTS.NEW.FONT_DATA,
		scale = DEFAULTS.NEW.SCALE,
	}
	function NewButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			textColor = _Value(self.defaultProps.textColor),
			disabledTextColor = _Value(self.defaultProps.disabledTextColor),
			isEnabled = _Value(self.defaultProps.isEnabled),
			elevation = _Value(self.defaultProps.elevation),
			schemeType = _Value(self.defaultProps.schemeType),
			fontData = _Value(self.defaultProps.fontData),
			scale = _Value(self.defaultProps.scale),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.new(
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.textColor,
			self._internalStates.disabledTextColor,
			self._internalStates.isEnabled,
			self._internalStates.elevation,
			self._internalStates.schemeType,
			self._internalStates.fontData,
			self._internalStates.scale
		)
	end

	function NewButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function NewButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function NewButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.New = NewButtonOutlinedButton
end

do
	local OnPrimaryButtonOutlinedButton = React.Component:extend("OnPrimaryButtonOutlinedButton")
	OnPrimaryButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_PRIMARY.STYLE,
		onClick = DEFAULTS.ON_PRIMARY.ON_CLICK,
		text = DEFAULTS.ON_PRIMARY.TEXT,
		icon = DEFAULTS.ON_PRIMARY.ICON,
		elevation = DEFAULTS.ON_PRIMARY.ELEVATION,
		isEnabled = DEFAULTS.ON_PRIMARY.IS_ENABLED,
	}
	function OnPrimaryButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onPrimary(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnPrimaryButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnPrimaryButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnPrimaryButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnPrimary = OnPrimaryButtonOutlinedButton
end

do
	local OnSecondaryButtonOutlinedButton = React.Component:extend("OnSecondaryButtonOutlinedButton")
	OnSecondaryButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_SECONDARY.STYLE,
		onClick = DEFAULTS.ON_SECONDARY.ON_CLICK,
		text = DEFAULTS.ON_SECONDARY.TEXT,
		icon = DEFAULTS.ON_SECONDARY.ICON,
		elevation = DEFAULTS.ON_SECONDARY.ELEVATION,
		isEnabled = DEFAULTS.ON_SECONDARY.IS_ENABLED,
	}
	function OnSecondaryButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onSecondary(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnSecondaryButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnSecondaryButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnSecondaryButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnSecondary = OnSecondaryButtonOutlinedButton
end

do
	local OnTertiaryButtonOutlinedButton = React.Component:extend("OnTertiaryButtonOutlinedButton")
	OnTertiaryButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_TERTIARY.STYLE,
		onClick = DEFAULTS.ON_TERTIARY.ON_CLICK,
		text = DEFAULTS.ON_TERTIARY.TEXT,
		icon = DEFAULTS.ON_TERTIARY.ICON,
		elevation = DEFAULTS.ON_TERTIARY.ELEVATION,
		isEnabled = DEFAULTS.ON_TERTIARY.IS_ENABLED,
	}
	function OnTertiaryButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onTertiary(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnTertiaryButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnTertiaryButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnTertiaryButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnTertiary = OnTertiaryButtonOutlinedButton
end

do
	local OnPrimaryContainerButtonOutlinedButton =
		React.Component:extend("OnPrimaryContainerButtonOutlinedButton")
	OnPrimaryContainerButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_PRIMARY_CONTAINER.STYLE,
		onClick = DEFAULTS.ON_PRIMARY_CONTAINER.ON_CLICK,
		text = DEFAULTS.ON_PRIMARY_CONTAINER.TEXT,
		icon = DEFAULTS.ON_PRIMARY_CONTAINER.ICON,
		elevation = DEFAULTS.ON_PRIMARY_CONTAINER.ELEVATION,
		isEnabled = DEFAULTS.ON_PRIMARY_CONTAINER.IS_ENABLED,
	}
	function OnPrimaryContainerButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onPrimaryContainer(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnPrimaryContainerButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnPrimaryContainerButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnPrimaryContainerButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnPrimaryContainer = OnPrimaryContainerButtonOutlinedButton
end

do
	local OnSecondaryContainerButtonOutlinedButton =
		React.Component:extend("OnSecondaryContainerButtonOutlinedButton")
	OnSecondaryContainerButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_SECONDARY_CONTAINER.STYLE,
		onClick = DEFAULTS.ON_SECONDARY_CONTAINER.ON_CLICK,
		text = DEFAULTS.ON_SECONDARY_CONTAINER.TEXT,
		icon = DEFAULTS.ON_SECONDARY_CONTAINER.ICON,
		elevation = DEFAULTS.ON_SECONDARY_CONTAINER.ELEVATION,
		isEnabled = DEFAULTS.ON_SECONDARY_CONTAINER.IS_ENABLED,
	}
	function OnSecondaryContainerButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onSecondaryContainer(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnSecondaryContainerButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnSecondaryContainerButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnSecondaryContainerButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnSecondaryContainer = OnSecondaryContainerButtonOutlinedButton
end

do
	local OnTertiaryContainerButtonOutlinedButton =
		React.Component:extend("OnTertiaryContainerButtonOutlinedButton")
	OnTertiaryContainerButtonOutlinedButton.defaultProps = {
		style = DEFAULTS.ON_TERTIARY_CONTAINER.STYLE,
		onClick = DEFAULTS.ON_TERTIARY_CONTAINER.ON_CLICK,
		text = DEFAULTS.ON_TERTIARY_CONTAINER.TEXT,
		icon = DEFAULTS.ON_TERTIARY_CONTAINER.ICON,
		elevation = DEFAULTS.ON_TERTIARY_CONTAINER.ELEVATION,
		isEnabled = DEFAULTS.ON_TERTIARY_CONTAINER.IS_ENABLED,
	}
	function OnTertiaryContainerButtonOutlinedButton:init(initialProps: { [string]: unknown })
		self._maid = Maid.new()
		local _fuse = ColdFusion.fuse(self._maid)
		local _Value = _fuse.Value
		self._internalStates = {
			style = _Value(self.defaultProps.style),
			onClick = _Value(self.defaultProps.onClick),
			text = _Value(self.defaultProps.text),
			icon = _Value(self.defaultProps.icon),
			elevation = _Value(self.defaultProps.elevation),
			isEnabled = _Value(self.defaultProps.isEnabled),
		}
		table.freeze(self._internalStates)
		for k, v in pairs(initialProps) do
			if self._internalStates[k] then
				self._internalStates[k]:Set(v)
			end
		end
		self._instance = Source.onTertiaryContainer(
			self._internalStates.style,
			self._internalStates.onClick,
			self._internalStates.text,
			self._internalStates.icon,
			self._internalStates.elevation,
			self._internalStates.isEnabled
		)
	end

	function OnTertiaryContainerButtonOutlinedButton:render()
		local props = {
			target = self._instance,
		}
		for k, v in pairs(self.props) do
			if self.props[k] ~= v then
				if self._internalStates[k] then
					if v == nil then
						v = self.defaultProps[k]
					end
					self._internalStates[k]:Set(v)
				else
					props[k] = v
				end
			end
		end
		return React.createElement(ReactRoblox.createPortal, props)
	end
	function OnTertiaryContainerButtonOutlinedButton:componentDidMount()
		for k, v in pairs(self) do
			if typeof(v) == "table" then
				if v["virtualNode"] then
					if self._instance.Parent ~= v["virtualNode"]["hostParent"] then
						self._instance.Parent = v["virtualNode"]["hostParent"]
					end
					break
				end
			end
		end
	end
	function OnTertiaryContainerButtonOutlinedButton:willUnmount()
		self._maid:Destroy()
		self._instance:Destroy()
	end
	Interface.OnTertiaryContainer = OnTertiaryContainerButtonOutlinedButton
end

return Interface
