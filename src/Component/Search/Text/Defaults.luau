--!strict
-- this script was generated by the build system, please do not make manual edits
local _Package = script.Parent.Parent.Parent.Parent
local _Packages = _Package.Parent
-- Services
-- Packages
local Maid = require(_Packages:WaitForChild("Maid"))
local ColdFusion = require(_Packages:WaitForChild("ColdFusion"))

-- Modules
local Types = require(_Package:WaitForChild("Types"))
local Style = require(_Package:WaitForChild("Style"))
local Enums = require(_Package:WaitForChild("Enums"))

-- Types
type Maid = Maid.Maid
type Style = Style.Style
type FontData = Types.FontData
type OptionData = Types.OptionData
type ButtonData = Types.ButtonData
type ImageData = Types.ImageData
type CanBeState<V> = ColdFusion.CanBeState<V>

return {
	NEW = {
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		TEXT_COLOR = Color3.new(),
		BACKGROUND_COLOR = Color3.new(),
		ELEVATION = 0,
		SCHEME_TYPE = Enums.SchemeType.Light,
		FONT_DATA = Types.newFontData(Font.fromEnum(Enum.Font.ArialBold), 14),
		SCALE = 1,
	},
	ON_PRIMARY = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
	ON_SECONDARY = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
	ON_TERTIARY = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
	ON_PRIMARY_CONTAINER = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
	ON_SECONDARY_CONTAINER = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
	ON_TERTIARY_CONTAINER = {
		STYLE = Style.new(1, "Arial", "Light", Color3.new(0, 0.4, 0.7)),
		ON_INPUT_ENTERED = function(input: string)
			return input
		end,
		LABEL = nil :: string?,
		OPTION_SOLVER = function(input: string)
			return {}
		end,
		OPTION_CONSTRUCTOR = nil :: ((key: string, onClick: (key: string) -> ()) -> GuiObject)?,
		ELEVATION = nil :: number?,
	},
}
