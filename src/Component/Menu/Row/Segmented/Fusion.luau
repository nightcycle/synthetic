--!strict
-- this script was generated by the build system, please do not make manual edits
local _Package = script.Parent.Parent.Parent.Parent.Parent
local _Packages = _Package.Parent
-- Services
-- Packages
local Maid = require(_Packages:WaitForChild("Maid"))
local ColdFusion = require(_Packages:WaitForChild("ColdFusion"))

-- Modules
local Types = require(_Package:WaitForChild("Types"))
local Style = require(_Package:WaitForChild("Style"))
local Enums = require(_Package:WaitForChild("Enums"))
local Translators = require(_Package:WaitForChild("Translators"))

local Source = require(script.Parent:WaitForChild("ColdFusion"))

-- Types
type Maid = Maid.Maid
type Style = Style.Style
type FontData = Types.FontData
type OptionData = Types.OptionData
type ButtonData = Types.ButtonData
type ImageData = Types.ImageData
type CanBeState<V> = ColdFusion.CanBeState<V>
type FusionCanBeState<V> = Translators.FusionCanBeState<V>

-- Constants
-- Variables
-- References
-- Private Functions
-- Class
local Interface = {}

function Interface.new(
	options: FusionCanBeState<{ [number]: OptionData }>,
	density: FusionCanBeState<number?>,
	isMultiSelect: FusionCanBeState<boolean?>,
	textColor: FusionCanBeState<Color3>,
	fillTextColor: FusionCanBeState<Color3>,
	fillBackgroundColor: FusionCanBeState<Color3>,
	outlineColor: FusionCanBeState<Color3>,
	elevation: FusionCanBeState<number?>,
	schemeType: FusionCanBeState<Enums.SchemeType>,
	fontData: FusionCanBeState<FontData>,
	scale: FusionCanBeState<number?>
): GuiObject
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local function convert<V>(value: FusionCanBeState<V>): CanBeState<V>
		return Translators.Fusion.toColdFusion(maid, _fuse, value)
	end
	local inst = Source.new(
		convert(options),
		convert(density),
		convert(isMultiSelect),
		convert(textColor),
		convert(fillTextColor),
		convert(fillBackgroundColor),
		convert(outlineColor),
		convert(elevation),
		convert(schemeType),
		convert(fontData),
		convert(scale)
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	return inst
end

function Interface.primaryContainer(
	style: FusionCanBeState<Style>,
	options: FusionCanBeState<{ [number]: OptionData }>,
	density: FusionCanBeState<number?>,
	isMultiSelect: FusionCanBeState<boolean?>,
	elevation: FusionCanBeState<number?>
): GuiObject
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local function convert<V>(value: FusionCanBeState<V>): CanBeState<V>
		return Translators.Fusion.toColdFusion(maid, _fuse, value)
	end
	local inst = Source.primaryContainer(
		convert(style),
		convert(options),
		convert(density),
		convert(isMultiSelect),
		convert(elevation)
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	return inst
end

function Interface.secondaryContainer(
	style: FusionCanBeState<Style>,
	options: FusionCanBeState<{ [number]: OptionData }>,
	density: FusionCanBeState<number?>,
	isMultiSelect: FusionCanBeState<boolean?>,
	elevation: FusionCanBeState<number?>
): GuiObject
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local function convert<V>(value: FusionCanBeState<V>): CanBeState<V>
		return Translators.Fusion.toColdFusion(maid, _fuse, value)
	end
	local inst = Source.secondaryContainer(
		convert(style),
		convert(options),
		convert(density),
		convert(isMultiSelect),
		convert(elevation)
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	return inst
end

function Interface.tertiaryContainer(
	style: FusionCanBeState<Style>,
	options: FusionCanBeState<{ [number]: OptionData }>,
	density: FusionCanBeState<number?>,
	isMultiSelect: FusionCanBeState<boolean?>,
	elevation: FusionCanBeState<number?>
): GuiObject
	local maid = Maid.new()
	local _fuse = ColdFusion.fuse(maid)
	local function convert<V>(value: FusionCanBeState<V>): CanBeState<V>
		return Translators.Fusion.toColdFusion(maid, _fuse, value)
	end
	local inst = Source.tertiaryContainer(
		convert(style),
		convert(options),
		convert(density),
		convert(isMultiSelect),
		convert(elevation)
	)
	maid:GiveTask(inst.Destroying:Connect(function()
		maid:Destroy()
	end))
	return inst
end

return Interface
